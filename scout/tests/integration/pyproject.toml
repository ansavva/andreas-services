[tool.poetry]
name = "scout-integration-tests"
version = "0.1.0"
description = "Scout integration test suite"
authors = []
packages = []

[tool.poetry.dependencies]
python = "^3.11"

[tool.poetry.group.dev.dependencies]
pytest = "7.4.3"
requests = "2.31.0"
boto3 = "1.34.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
