name: Humbugg · PR

on:
  pull_request:
    paths:
      - 'humbugg/**'
      - '.github/workflows/humbugg-pr.yml'

env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: humbugg-backend-production

jobs:
  # ---------------------------------------------------------------------------
  # Tier 0 – validate Terraform configuration
  # ---------------------------------------------------------------------------
  validate-terraform:
    name: Validate Terraform
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v7

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Setup tflint
        uses: terraform-linters/setup-tflint@v4
        with:
          tflint_version: v0.62.0

      - name: Terraform fmt check
        run: terraform -chdir=humbugg/infra/envs/prod fmt -check -recursive ../../

      - name: Terraform validate
        run: |
          terraform -chdir=humbugg/infra/envs/prod init -backend=false -input=false
          terraform -chdir=humbugg/infra/envs/prod validate

      - name: Test canonical redirect
        run: node --test humbugg/infra/modules/hosting/canonical_redirect.test.mjs

      - name: tflint init
        run: tflint --init --config "$(pwd)/.tflint.hcl"

      - name: tflint
        run: tflint --recursive --chdir=humbugg/infra --config "$(pwd)/.tflint.hcl"

  validate-backend:
    name: Validate .NET Backend
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v7
      - name: Set up .NET 10
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'
      - name: Restore
        run: dotnet restore humbugg/backend/Humbugg.slnx
      - name: Format check
        run: dotnet format humbugg/backend/Humbugg.slnx --verify-no-changes --no-restore
      - name: Test
        run: |
          cd humbugg/backend
          dotnet test Humbugg.slnx --configuration Release --no-restore

  # ---------------------------------------------------------------------------
  # Tier 1 – verify Docker build + frontend build (no push, no deploy)
  # ---------------------------------------------------------------------------
  build-backend:
    name: Build Backend Docker Image
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v7

      - name: Build Docker image (verify only, no push)
        run: |
          cd humbugg/backend
          DOCKER_BUILDKIT=0 docker build -t humbugg-backend:ci-verify .

      - name: Smoke test backend runtime and health
        run: |
          docker run --rm -d \
            --name humbugg-backend-smoke \
            -p 5088:8080 \
            --entrypoint dotnet \
            -e ASPNETCORE_URLS=http://+:8080 \
            humbugg-backend:ci-verify Humbugg.Api.dll
          trap 'docker rm -f humbugg-backend-smoke >/dev/null 2>&1 || true' EXIT
          for attempt in {1..20}; do
            if curl --fail --silent --show-error http://127.0.0.1:5088/health | grep -q '"status":"ok"'; then
              exit 0
            fi
            sleep 1
          done
          docker logs humbugg-backend-smoke
          exit 1

  build-frontend:
    name: Build SSR Frontend
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read

    steps:
      - uses: actions/checkout@v7

      - name: Set up Node.js 24
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@ansavva'
          cache: npm
          cache-dependency-path: humbugg/frontend/package-lock.json

      - name: Install dependencies
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: cd humbugg/frontend && npm ci

      - name: Typecheck, test, and build
        run: |
          cd humbugg/frontend
          npm run typecheck
          npm test
          npm run build

      - name: Build SSR Lambda image
        run: cd humbugg/frontend && docker build -t humbugg-frontend:ci-verify .

      - name: Smoke test SSR Lambda handler import
        run: |
          docker run --rm --entrypoint node humbugg-frontend:ci-verify \
            -e "import('./server/index.mjs').then(({ handler }) => { if (typeof handler !== 'function') process.exit(1); console.log('SSR handler ready'); })"
