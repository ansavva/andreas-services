name: storybook
services:
  dynamodb:
    image: amazon/dynamodb-local:latest
    command: ["-jar", "DynamoDBLocal.jar", "-sharedDb", "-dbPath", "/data"]
    user: root
    ports:
      - "8004:8000"
    volumes:
      - dynamodb-local:/data

volumes:
  dynamodb-local:
