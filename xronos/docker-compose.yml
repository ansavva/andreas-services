version: "3.9"

services:
  web:
    build:
      context: ./web
    ports:
      - "5173:5173"
    environment:
      - VITE_API_BASE_URL=http://localhost:4000
    depends_on:
      - api

  api:
    build:
      context: ./api
    ports:
      - "4000:4000"
